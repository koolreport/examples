The example demonstrates usage of `Join`, `LeftJoin`, `RightJoin`, `FullJoin` process. This Jjin processes is used to join two source of data by matching id between them. The result is the combination of both.
